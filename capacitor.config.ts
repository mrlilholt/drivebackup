import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'come.lilholt.lilholtdrives',
  appName: 'LilholtDrives',
  webDir: 'build'
};

export default config;
